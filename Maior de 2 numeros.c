#include <stdio.h>

int main()
{
int x, y, maior;
    printf("");
    scanf("%d", &x);
    
    printf("");
    scanf("%d", &y);
    
    if (x > y)
        printf("%d",x);
    else
        printf("%d",y);

return 0;
}
